# The fuzzer always builds against the madsim deterministic simulator, so a
# crash reproduces exactly from its input. `--cfg madsim` swaps the tokio alias
# for the simulator and routes randomness/time through the seeded runtime.
[build]
rustflags = ["--cfg", "madsim"]
rustdocflags = ["--cfg", "madsim"]
